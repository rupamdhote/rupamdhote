- name: Playbook1
  hosts: localhost
  #vars:
    #URL: cloudblitz.in
  vars_prompt:
   - name: URL
     prompt: "Enter Anything"
     private: no  
  tasks:
   - name: Print messages_1
     debug:
     msg: "{{URL}}"
